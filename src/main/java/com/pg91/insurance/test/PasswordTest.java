package com.pg91.insurance.test;

import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
public class PasswordTest {
    public static void main(String[] args) {
//        BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();
//        String password = "Dineth";
//        String encoded = encoder.encode(password);
//
//        System.out.println("Original password: " + password);
//        System.out.println("BCrypt encoded: " + encoded);
//
//        // Test if the encoded password matches
//        boolean matches = encoder.matches(password, encoded);
//        System.out.println("Password matches: " + matches);
//
//        // Test with a known BCrypt hash (you can replace this with your actual hash from DB)
//        String knownHash = "$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iKTVEFDi";
//        boolean knownMatches = encoder.matches(password, knownHash);
//        System.out.println("Known hash matches: " + knownMatches);




//        BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();
//        boolean matches = encoder.matches("your-guessed-plaintext-password", loadedUser.getPassword());
//        System.out.println("Password matches: " + matches);
    }
}
